// both double and stand buttons should disable all buttons after being pressed (can change styling)
// move buttons into header?

// INDEX
const userChips = document.querySelector(".user-chips");
const playerAnte = prompt{
    How much do you want to bet?
    Current chip count: ${singleDeckGame.getUserChips()
    }};

userChips.textContent = playerAnte;
singleDeckGame.playerAnte(playerAnte);

// "actions" container in html for all buttons - disables buttons after click (see line 1) 
// - pull out into its own anonymous function 

// attribute styles
// [disabled="true"]    -    html

// DOM TEST
